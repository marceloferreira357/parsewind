import { clazzArrayToMap } from "../../common/utils";

export const isolation = clazzArrayToMap([
  { clazz: "isolate", properties: "isolation: isolate;" },
  { clazz: "isolation-auto", properties: "isolation: auto;" },
]);
