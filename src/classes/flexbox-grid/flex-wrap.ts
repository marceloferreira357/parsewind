import { clazzArrayToMap } from "../../common/utils";

export const flexWrap = clazzArrayToMap([
  { clazz: "flex-wrap", properties: "flex-wrap: wrap;" },
  { clazz: "flex-wrap-reverse", properties: "flex-wrap: wrap-reverse;" },
  { clazz: "flex-nowrap", properties: "flex-wrap: nowrap;" },
]);
