import { clazzArrayToMap } from "../../common/utils";

export const flexGrow = clazzArrayToMap([
  { clazz: "grow", properties: "flex-grow: 1;" },
  { clazz: "grow-0", properties: "flex-grow: 0;" },
]);
