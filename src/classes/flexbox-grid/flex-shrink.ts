import { clazzArrayToMap } from "../../common/utils";

export const flexShrink = clazzArrayToMap([
  { clazz: "shrink", properties: "flex-shrink: 1;" },
  { clazz: "shrink-0", properties: "flex-shrink: 0;" },
]);
