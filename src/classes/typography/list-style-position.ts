import { clazzArrayToMap } from "../../common/utils";

export const listStylePosition = clazzArrayToMap([
  { clazz: "list-inside", properties: "list-style-position: inside;" },
  { clazz: "list-outside", properties: "list-style-position: outside;" },
]);
